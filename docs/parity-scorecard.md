# Cursor PR Agent Engine Parity Scorecard

This maintainer-facing scorecard tracks clean-room Cursor-native capabilities from
planning ID to implementation evidence, fixture coverage, diagnostics, and release
readiness. It is intentionally not a public claim of full PR-Agent parity.

## Capability status

| Capability ID | Level | Implementation status | Fixture / test evidence | Diagnostics status | Release blocker | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| CMD-REVIEW-P0 | P0 | Implemented in `scripts/engine/commands.py` and `scripts/engine/prompts.py` | `tests/fixtures/pr_regression/docs_only_no_findings`, `tests/fixtures/pr_regression/security_finding`; `tests/test_engine.py::CommandTests` | Command name and run metadata render in PR diagnostics | No | Preserves `/cursor-review` as the default command. |
| CMD-ASK-P1 | P1 | Implemented behind command enablement in `scripts/engine/commands.py` and prompt templates | `tests/fixtures/pr_regression/ask_question`; command routing unit tests | Command marker and schema diagnostics are available when enabled | No for v0.5; verify UX before stable v1 | P1 command remains opt-in through `enabled_commands`. |
| CMD-IMPROVE-P1 | P1 | Template and schema are present in `scripts/engine/prompt_templates/improve.md` and `scripts/engine/schemas.py` | `tests/fixtures/pr_regression/improve_suggestions`; command schema unit coverage | Command marker, schema diagnostics, quality-gate decision, and repo-guidance diagnostics are available when enabled | No for fixture coverage; dogfooding evidence still needed before stable release | Concrete fixture verifies author-facing suggestions without repeating review findings. |
| CMD-DESCRIBE-P1 | P1 | Implemented as comment-only describe output | `tests/fixtures/pr_regression/describe_summary` | Command marker and schema diagnostics are available when enabled | No for comment-only mode | PR body mutation remains deferred. |
| CTX-METADATA-P0 | P0 | Implemented in `scripts/engine/context.py` | `tests/fixtures/pr_regression/docs_only_no_findings`; `tests/test_engine.py::ContextBuilderTests` | Diagnostics show metadata presence without leaking PR body | No | Includes PR title/body presence, commit messages, changed files, and diff stat. |
| DIFF-LARGE-P0 | P0 | Implemented in `scripts/engine/diff_selector.py` | `tests/fixtures/pr_regression/large_partial_review`; diff selector unit tests | Reviewed/skipped file counts and truncation state render in diagnostics | No | Byte-aware selection exists; token-aware fitting remains a later improvement. |
| DIFF-GENERATED-P0 | P0 | Implemented in `scripts/engine/diff_selector.py` with default generated/lockfile skipping and config override | `tests/fixtures/pr_regression/generated_lockfile_skipped`; `tests/test_engine.py::DiffSelectorTests::test_build_diff_skips_generated_and_lockfiles_by_default` | Skipped files use `generated_or_lockfile` reason and rendered diagnostics report skipped-file counts | No | Common lockfiles, minified assets, and generated output directories are excluded from model evidence by default. |
| DIFF-INDEX-P0 | P0 | Implemented in `scripts/engine/diff_index.py` and attached to selected diff metadata | `tests/test_engine.py::FindingGroundingTests`; `tests/fixtures/pr_regression/invalid_finding_anchor` | Grounding diagnostics report diff-index schema, indexed file count, and anchor counts | No | Indexes selected-diff new-side and old-side changed lines before Cursor findings are rendered. |
| FINDING-GROUNDING-P0 | P0 | Implemented in `scripts/engine/grounding.py` and applied after structured parsing in live, dry-run, and fixture paths | `tests/test_engine.py::FindingGroundingTests`; `tests/fixtures/pr_regression/security_finding`; `tests/fixtures/pr_regression/invalid_finding_anchor` | Rendered diagnostics include anchored, file-only, unanchored, invalid, and skipped-file finding counts | No | Invalid anchors are downgraded and suppressed in `findings-json`; file-only findings are marked for human verification. |
| FINDING-DEDUP-P0 | P0 | Implemented in `scripts/engine/findings.py` and applied after grounding in live, dry-run, and fixture paths | `tests/test_engine.py::FindingDedupTests`; `tests/fixtures/pr_regression/duplicate_findings` | Rendered diagnostics include finding-dedup schema, input count, duplicate count, cap count, and output count | No | Same-run duplicates collapse before `max_findings`; sorting prefers severity, confidence, grounding quality, and actionability. |
| OUTPUT-QUALITY-GATE-P0 | P0 | Implemented in `scripts/engine/quality_gate.py` and applied before CI policy evaluation and rendering in live, dry-run, and fixture paths | `tests/test_engine.py::OutputQualityGateTests`; `tests/fixtures/quality_gate/unsupported_claim` | Rendered diagnostics include quality-gate schema, publish decision, reason, coverage, publishable/suppressed counts, unsupported-claim count, and redaction status | No | Deterministic local gate emits `publish`, `publish_partial`, `publish_with_diagnostics`, `suppress_findings`, or `fail_before_publish`; unsupported external claims are downgraded instead of published as high-confidence findings. |
| SCOPE-FULL-P0 | P0 | Implemented in `scripts/engine/scope.py` and applied before include/exclude filtering in `scripts/engine/diff_selector.py` | `tests/test_engine.py::ReviewScopeTests`; `tests/test_engine.py::DiffSelectorTests` | Review scope schema, mode, selected count, skipped count, and warnings render in diagnostics | No | Default remains the full selected PR diff. |
| SCOPE-FILES-P1 | P1 | Implemented through allowlisted `--files`, `--scope`, `scope-mode`, and `scope-files` controls | `tests/test_engine.py::CommandTests`; `tests/test_engine.py::ReviewScopeTests`; `tests/test_engine.py::DiffSelectorTests` | Command-scoped file decisions are explicit and skipped files use `scope_not_requested` | No for command-scoped files; since-last-run incremental remains deferred | Opt-in file scope can narrow review to repository-relative paths or globs without shell parsing. |
| PROMPT-REVIEW-P0 | P0 | Implemented in `scripts/engine/prompts.py` and `scripts/engine/prompt_templates/review.md` | `tests/fixtures/pr_regression/docs_only_no_findings`, `tests/fixtures/pr_regression/security_finding` | Prompt diagnostics include command, model, config, diff coverage, and schema version | No | Clean-room prompt text is versioned in this repository. |
| PROMPT-COMMAND-SCHEMA-P0 | P0 | Implemented in `scripts/engine/prompt_templates/*.md` and `scripts/engine/schemas.py` | `tests/fixtures/pr_regression/ask_question`, `tests/fixtures/pr_regression/describe_summary`, `tests/fixtures/pr_regression/improve_suggestions`; prompt parser tests | Diagnostics include command-specific schema contract and prompt template version | No for command-template fixture coverage | Review, ask, improve, and describe now have concrete prompt/schema evidence. |
| PARSER-FALLBACK-P0 | P0 | Implemented in `scripts/engine/parser.py` | Review fixtures and parser unit tests | Parser diagnostics are attached to rendered comment diagnostics | No | Invalid JSON degrades to markdown while preserving diagnostics. |
| PARSER-COMMAND-SCHEMA-P1 | P1 | Implemented in `scripts/engine/schemas.py` and `scripts/engine/parser.py` | `tests/fixtures/pr_regression/ask_question`, `tests/fixtures/pr_regression/describe_summary`, `tests/fixtures/pr_regression/improve_suggestions`; parser unit tests | Parsed output records command-specific schema status and fallback diagnostics | No for command-schema fixtures; dogfooding evidence still needed before stable release | Ask, improve, and describe use command-specific structured output contracts. |
| PARSER-RETRY-P1 | P1 | Implemented in `scripts/engine/parser.py` and orchestrated by `scripts/cursor_review.py` | Parser retry unit tests; `tests/fixtures/pr_regression/invalid_model_output` covers invalid model output fallback | Retry count and parser failure kind are available in diagnostics | No for P1 | One repair prompt is used before markdown fallback in live runs; the no-Cursor fixture verifies malformed JSON remains diagnostic-only. |
| SCHEMA-EVOLUTION-P1 | P1 | Implemented in `scripts/engine/schemas.py`, parser compatibility checks, and `docs/config-migration.md` | `tests/test_engine.py` schema compatibility and render diagnostics tests | Parser diagnostics report schema compatibility version, payload status, payload version, and command match | No for P1; release notes still needed before tagging | Current schema is accepted, missing legacy schema versions are diagnosed, and unsupported versions enter retry/fallback. |
| HUMAN-WORKFLOW-P1 | P1 | Implemented in `docs/human-review-workflow.md`, README guidance, and rendered comment policy text | `tests/test_engine.py::PromptParserRenderTests::test_render_comment_declares_human_advisory_policy` | Review comments report `advisory_non_blocking` and `Human decision required: true` | No | AI findings are advisory by default; the action does not approve, merge, or block PRs unless maintainers add separate policy. |
| CI-POLICY-P0 | P0 | Implemented in `scripts/engine/ci_policy.py`, `scripts/cursor_review.py`, action outputs, and `docs/ci-policy.md` | `tests/test_engine.py::CIPolicyTests`; render and entrypoint output tests | `ci-policy-json` and rendered diagnostics report default policy, fail-on-error, fail-on-findings status, workflow exit code, and high-severity finding count | No | Findings remain non-blocking by default; `fail-on-findings` is diagnosed as reserved until severity-threshold gating is safe to implement. |
| CONFIG-SCHEMA-P1 | P1 | Implemented in `scripts/engine/config.py` with repo-config unknown-key filtering, invalid-value safe fallbacks, and stable config diagnostics | `tests/fixtures/config_invalid/bad_values`; `tests/test_engine.py::ConfigTests::test_invalid_config_fixture_reports_safe_fallbacks`; render diagnostics coverage | Rendered comments report config schema, loaded state, unknown-key count, fallback count, and safe warning messages | No for current config surface; full JSON schema file remains a final hardening candidate | Invalid config values are diagnosed before Cursor is contacted without exposing token-like raw values. |
| LOCAL-DRY-RUN-P0 | P0 | Implemented in `scripts/engine/local_dry_run.py`, `scripts/cursor_review.py --dry-run`, and `docs/local-development.md` | `tests/test_engine.py::EntrypointTests::test_main_local_dry_run_builds_prompt_and_render_without_cursor` | Dry-run diagnostics report `local_dry_run`, `cursor_contacted=false`, selected diff coverage, parser status, and CI policy | No | Validates config, local git context, diff selection, prompt rendering, stored output parsing, and comment rendering without `CURSOR_API_KEY`. |
| LOCALIZATION-P0 | P0 | Implemented in `scripts/engine/localization.py`, prompt language instructions, rendered diagnostics, README examples, and `docs/localization.md` | `tests/test_engine.py::ConfigTests`; `tests/test_engine.py::PromptParserRenderTests` | Localization diagnostics report stable schema, effective language, fallback state, and reason with English keys | No | `language` affects human-readable prose only; JSON schema keys and diagnostics remain stable. |
| ACCEPTANCE-RUBRIC-P1 | P1 | Implemented in `docs/acceptance-rubric.md`, comparison report template fields, and fixture `human_eval.md` records | `tests/test_engine.py::AcceptanceRubricTests`; `tests/fixtures/pr_regression/*/human_eval.md` | Rubric records false positives, missed issues, evidence quality, command intent, conciseness, diagnostics usefulness, skipped-content transparency, and follow-up action | No for rubric; continued dogfooding records remain a stable-release evidence gate | Converts human usefulness observations into fixture, prompt, parser, docs, backlog, or non-goal follow-up. |
| FINDING-TAXONOMY-P0 | P0 | Implemented in `scripts/engine/taxonomy.py`, `scripts/engine/schemas.py`, and parser normalization | `tests/fixtures/pr_regression/security_finding`, `tests/fixtures/pr_regression/style_noise_suppressed`; taxonomy parser unit tests | Taxonomy schema, normalized count, invalid fields, and noise suppression render in diagnostics | No for taxonomy; grounding and output quality gate remain separate blockers | Stable categories, severity, confidence, schema version, and review noise-control are deterministic. |
| PUB-PERSISTENT-P0 | P0 | Implemented through command-specific markers in `scripts/engine/run_state.py` and rendering outputs | Review and large-partial fixtures; entrypoint unit tests | Outputs include comment marker and run metadata JSON | No | Summary comment publishing remains the P0 surface; inline comments are deferred. |
| CURSOR-RUNNER-P0 | P0 | Implemented in `scripts/engine/runner.py` | `tests/test_engine.py::RunnerContractTests` | Failure kind, requested model, timeout, retry count, and cursor-call budget are reported | No | Cursor CLI remains the execution layer. |
| BUDGET-CONTROLS-P1 | P1 | Implemented in `scripts/engine/budget.py`, `scripts/engine/diff_selector.py`, and `scripts/engine/runner.py` | `tests/fixtures/pr_regression/large_partial_review`; budget and runner unit tests | Budget limits, skipped files, and cursor-call attempts are visible | No | `max_cursor_calls` defaults to one. |
| RUN-STATE-P0 | P0 | Implemented in `scripts/engine/run_state.py`, `scripts/engine/lifecycle.py`, and entrypoint outputs | `tests/test_engine.py::RunStateTests`, `tests/test_engine.py::ReviewLifecycleTests`, and entrypoint tests | Run id, command marker, lifecycle final state, head SHA, event name, and stale diagnostics are output | No | P0 stale-run support is diagnostic-only; progress comments remain P1. |
| SEC-TRIGGER-P0 | P0 | Implemented in `scripts/engine/trust_policy.py` before context construction or Cursor calls | `tests/fixtures/triggers/*.json`; trigger policy unit tests | Skip reason, trust level, event, fork state, and secret availability are reported | No | Untrusted issue comments and fork PRs without secrets do not call Cursor. |
| SEC-PRIVACY-P0 | P0 | Implemented in `scripts/engine/redaction.py`, live-run artifact gating, and `docs/privacy-and-logging.md` | `tests/fixtures/privacy/token_like_output.json`; redaction, render, config, and entrypoint unit tests | Rendered comments report redaction schema/status/counts and debug artifact state | No | Live runs avoid raw prompt/raw output files by default; published surfaces use deterministic token-like redaction. |
| SUPPLY-CHAIN-P0 | P0 | Implemented in `scripts/engine/supply_chain.py`, `docs/dependencies.md`, and `docs/release-checklist.md` | `tests/test_engine.py::SupplyChainTests` | Stdlib-only import scan and action reference policy return machine-readable diagnostics | No | Stable releases require human review, tagged examples, dependency inventory review, and no auto-release. |
| FIXTURE-HARNESS-P0 | P0 | Implemented in `scripts/engine/fixtures.py` | `tests/test_engine.py::FixtureRegressionTests`; 15 concrete no-Cursor fixtures including `tests/fixtures/pr_regression/invalid_model_output`, `tests/fixtures/pr_regression/empty_cursor_output`, `tests/fixtures/pr_regression/improve_suggestions`, `tests/fixtures/pr_regression/deleted_line_anchor`, and `tests/fixtures/pr_regression/generated_lockfile_skipped` | Fixture diagnostics record fixture name, parser status, retry exhaustion, quality-gate status, and capability IDs | No for v0.5; v1 fixture volume remains open | Current harness has 13 PR regression fixtures plus 1 quality-gate fixture plus 1 config-invalid fixture. |
| COMPARISON-PROTOCOL-P1 | P1 | Implemented in `docs/parity-reports/README.md`, `template.md`, and initial gap log | `tests/test_engine.py::ComparisonProtocolTests` | Gap log requires release impact and backlog/deferred/non-goal decisions | No for protocol; sample volume remains a release gate | Initial suite maps 5 repository-owned samples and 6 gaps without copying PR-Agent output. |
| TRACEABILITY-SCORECARD-P0 | P0 | Implemented in this scorecard and validated by unit tests | `tests/test_engine.py::FixtureRegressionTests` scorecard coverage test | Capability IDs now connect fixture evidence to release blockers | No | Keep this file updated with each capability PR. |
| PROMPT-GOVERNANCE-P1 | P1 | Implemented in `docs/prompt-governance.md`, `scripts/engine/prompt_templates/VERSION`, and prompt diagnostics | `tests/test_engine.py::PromptParserRenderTests` prompt version and template contract tests | Prompt template version is included in prompt and PR comment diagnostics | No for governance; dogfooding metrics still feed later release gates | Prompt changes now have a checklist, metrics, fixture update rules, and clean-room guardrails. |
| REPO-GUIDANCE-P1 | P1 | Implemented in `scripts/engine/guidance.py`, `scripts/engine/context.py`, and `scripts/engine/prompts.py` | `tests/test_engine.py::RepoGuidanceTests`; `tests/fixtures/pr_regression/improve_suggestions`; `docs/repo-guidance.md` | Prompt diagnostics include loaded/skipped guidance metadata; rendered comments show file names and budget usage without guidance body | No for concrete fixture coverage; dogfooding evidence still needed before stable release | Loads `.cursor-review-instructions.md` for all commands and `best_practices.md` for `/cursor-improve` within repo-relative byte/line budgets. |
| HELP-DISCOVERABILITY-P1 | P1 | Implemented in `scripts/engine/help.py`, command routing, and the entrypoint static help fast path | `tests/test_engine.py::CommandTests`, `tests/test_engine.py::PromptParserRenderTests`, and entrypoint help coverage | Help diagnostics show enabled commands and `Cursor contacted: false` | No | `/cursor-help` and `/cursor-review help` publish safe static help and list only enabled Cursor-backed commands. |

## Current release blockers

These blockers are not failures of the scorecard itself; they are remaining final
product gates from `docs/plans/cursor-pr-agent-engine.plan.md`.

- `fixture-inventory`: the current suite has 15 concrete no-Cursor fixtures; the v0.5 volume gate is met and v1 still targets 20, with config-only/renamed/many-small-file and dogfooding-derived cases remaining high-priority final-product cases.
- `docs-positioning`: README positioning for clean-room Cursor-native behavior is not updated.

## Maintenance rules

- Add every new fixture capability ID to the scorecard before marking the related
  plan item complete.
- Do not mark a P0 capability as release-ready unless implementation, fixture or
  unit evidence, diagnostics, and release-blocker status are all recorded here.
- Keep P2/public parity claims out of the README until the stable-release checklist
  is complete.
